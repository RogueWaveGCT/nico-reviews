# Database file path (defaults to ./data/reviews.db)
DATABASE_PATH=./data/reviews.db

# Base URL for generating shareable links (no trailing slash)
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Optional: admin password for future auth
# ADMIN_PASSWORD=
